# API URL - Base URL del backend (sin /api al final)
# 
# NOTA: Esta variable es OPCIONAL. Si no se configura:
# - En desarrollo (localhost): se usará http://localhost:5000
# - En producción (cualquier otro dominio): se usará el mismo dominio del frontend
# 
# Solo necesitas configurar esta variable si quieres usar una URL diferente:
# REACT_APP_API_URL=http://localhost:5000
# 
# Ejemplos:
# Para desarrollo local: http://localhost:5000
# Para producción personalizada: https://fabricantes.asset-fy.com
